    <div class="mx-auto max-w-7xl space-y-6 px-4 py-8 sm:px-6 lg:px-10">
        <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-semibold uppercase tracking-[0.2em] text-moss">Community</p><h1 class="mt-1 font-display text-4xl font-semibold">Users</h1></div><input wire:model.live.debounce.300ms="search" type="search" placeholder="Search name or email" class="w-full rounded-xl border-0 bg-white px-4 py-3 text-sm shadow-sm ring-1 ring-ink/10 focus:ring-2 focus:ring-accent sm:w-72"></div>
        <div class="overflow-hidden rounded-2xl bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-ink text-paper/70"><tr><th class="px-5 py-4 font-medium">User</th><th class="px-5 py-4 font-medium">Campaigns</th><th class="px-5 py-4 font-medium">Joined</th><th class="px-5 py-4 font-medium">Access</th></tr></thead><tbody class="divide-y divide-ink/10">@forelse ($users as $user)<tr wire:key="user-{{ $user->id }}" class="hover:bg-paper/60"><td class="px-5 py-4"><p class="font-semibold">{{ $user->name }}</p><p class="mt-1 text-xs text-ink/50">{{ $user->email }}</p></td><td class="px-5 py-4 tabular-nums">{{ $user->campaigns_count }}</td><td class="px-5 py-4 text-ink/60">{{ $user->created_at->format('d M Y') }}</td><td class="px-5 py-4"><button type="button" wire:click="toggleAdmin({{ $user->id }})" class="rounded-lg bg-ink px-3 py-2 text-xs font-semibold text-white hover:bg-accent">Make admin</button></td></tr>@empty<tr><td colspan="4" class="px-5 py-12 text-center text-ink/55">No users match that search.</td></tr>@endforelse</tbody></table></div><div class="border-t border-ink/10 px-5 py-4">{{ $users->links() }}</div></div>
    </div>
